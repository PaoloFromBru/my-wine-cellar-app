specVersion: "v1"
serviceId: "my-wine-cellar-public"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "my-wine-cellar-public-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
